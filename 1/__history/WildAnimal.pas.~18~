unit WildAnimal;

interface

Uses Animal, Vcl.ExtCtrls;

type
  TWildAnimal = class(TAnimal)
  private
    FDanger: integer;
  public
    constructor Create(AIndex: integer; AColor: string; ADanger: integer; AImage: TImage);
  end;

implementation

constructor TWildAnimal.Create(AIndex: integer; AColor: string; ADanger: integer;
  AImage: TImage);
begin
  inherited Create(AIndex, AColor, AImage);
  FDanger := ADanger;
end;

end.
