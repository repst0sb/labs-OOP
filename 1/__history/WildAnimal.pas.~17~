unit WildAnimal;

interface

Uses Animal, Vcl.ExtCtrls;

type
  TWildAnimal = class(TAnimal)
  private
    FDanger: integer;
  public
    constructor Create(AColor: string; ADanger: integer; AImage: TImage);
  end;

implementation

constructor TWildAnimal.Create(AColor: string; ADanger: integer; AImage: TImage);
begin
  inherited Create(AColor, AImage);
  FDanger := ADanger;
end;

end.
