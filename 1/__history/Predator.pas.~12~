unit Predator;

interface

uses WildAnimal, Vcl.ExtCtrls;

type
  TPredator = class(TWildAnimal)
  private
    FPrey: string;
  public
    constructor Create(AColor: string; ADanger: integer; APrey: string;
      AImage: TImage);
  end;

implementation

constructor TPredator.Create(AColor: string; ADanger: integer; APrey: string;
  AImage: TImage);
begin
  inherited Create(AColor, ADanger, AImage);
  FPrey := APrey;
end;

end.
